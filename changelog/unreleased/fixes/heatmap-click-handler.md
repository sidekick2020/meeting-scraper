**Heatmap Click Handler**: Fixed heatmap clicks not responding
- Added CSS rule to make leaflet.heat canvas layer pass through pointer events
- Clicking on heatmap now properly zooms in and loads meetings for that area
- Cluster markers now receive click events correctly
