**API Logs Filters**: Added filtering capabilities to the API logs panel in dev mode
- Filter by status (Pending, Success, Error) - all shown by default when no filter is selected
- Filter by HTTP method (GET, POST, PUT, DELETE, etc.)
- Search filter for URL matching
- Clear filters button and filtered count indicator
