**Scrape History in Source Panel**: View scrape history when viewing a source
- Expandable "Scrape History" section in the Source Details panel
- Shows per-source statistics for each scrape run (found, saved, duplicates, errors)
- Lazy loads history data only when expanded
- Displays completion status and timestamp for each entry
