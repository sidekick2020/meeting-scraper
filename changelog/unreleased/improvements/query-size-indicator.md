**Query Size Indicator**: Loading indicator now shows accurate meeting count based on active filters
- Count updates dynamically as filters are added/changed
- State-level zoom now supports all filter parameters (day, type, online, hybrid, format, city, state)
- Backend endpoint `/api/meetings/by-state` now accepts filter parameters for accurate filtered counts
