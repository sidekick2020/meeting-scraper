**Map Loading Indicator**: Added subtle visual feedback when map data is being updated
- Shows a small spinner in the map header when fetching new meeting data
- Indicates when cached data is being refreshed in the background
- Provides better UX when panning/zooming the map while data loads
